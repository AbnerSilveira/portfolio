# RSA Visualizer — frontend-only; variáveis futuras (ex.: analytics) aqui.
NODE_ENV=development

# Opcional: URL do apps/web para o link "← Portfólio" no header (ex.: http://localhost:3000)
# NEXT_PUBLIC_PORTFOLIO_URL=http://localhost:3000
