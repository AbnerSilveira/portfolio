# URL base da app Next «rsa-visualizer» (iframe na página do projeto no portfólio).
# Desenvolvimento: http://localhost:3001 — produção: URL do deploy da demo (ex. Vercel).
NEXT_PUBLIC_RSA_VISUALIZER_URL=http://localhost:3001
