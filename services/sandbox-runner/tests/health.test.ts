import { describe, expect, it } from "vitest";

describe("sandbox-runner health", () => {
  it("is a placeholder test (runner is smoke-tested via /health)", () => {
    expect(true).toBe(true);
  });
});
